---
title: 工具全面测试报告
type: test-report
created: 2026-06-01
---

# AI 智能体工具全面测试

请按照以下步骤逐一执行所有 41 个工具，每个工具执行后记录结果。最后生成完整的测试报告。

---

## 第一阶段：系统与基础工具（5 个）

**步骤 1** — 调用 `get_vault_info`，获取当前 vault 基本信息
**步骤 2** — 调用 `get_current_datetime`，获取当前时间
**步骤 3** — 调用 `show_notice`，显示通知 "工具测试已开始"
**步骤 4** — 调用 `list_commands`，列出所有可用命令
**步骤 5** — 调用 `get_active_file`，获取当前活动文件信息

---

## 第二阶段：文件操作（9 个）

**步骤 6** — 调用 `write_file`，创建文件 `_AI测试/测试文件A.md`，内容为：
```markdown
---
status: active
tags: [测试, AI]
created: 2026-06-01
---
# 测试文件A
这是第一个测试文件。
- 任务1
- 任务2
- [ ] 待办事项1
- [ ] 待办事项2
- [x] 已完成事项
```

**步骤 7** — 调用 `read_file`，读取 `_AI测试/测试文件A.md` 的完整内容
**步骤 8** — 调用 `get_file_info`，获取 `_AI测试/测试文件A.md` 的元数据
**步骤 9** — 调用 `list_files`，列出 `_AI测试/` 目录下的所有文件
**步骤 10** — 调用 `append_file`，向 `_AI测试/测试文件A.md` 追加内容 "\n## 追加区域\n这是追加的内容。"
**步骤 11** — 调用 `edit_file`，将 `_AI测试/测试文件A.md` 中的 "任务1" 替换为 "任务1（已修改）"
**步骤 12** — 调用 `copy_file`，将 `_AI测试/测试文件A.md` 复制为 `_AI测试/测试文件B.md`
**步骤 13** — 调用 `read_file`，读取 `_AI测试/测试文件B.md` 确认复制成功
**步骤 14** — 调用 `rename_file`，将 `_AI测试/测试文件B.md` 重命名为 `_AI测试/测试文件C.md`

---

## 第三阶段：元数据操作（7 个）

**步骤 15** — 调用 `get_frontmatter`，读取 `_AI测试/测试文件A.md` 的 frontmatter
**步骤 16** — 调用 `set_frontmatter`，为 `_AI测试/测试文件A.md` 添加属性 `priority: high`
**步骤 17** — 调用 `get_frontmatter`，确认属性已添加
**步骤 18** — 调用 `get_tags`，获取 `_AI测试/测试文件A.md` 的标签列表
**步骤 19** — 调用 `get_tags`，获取整个 vault 的所有标签
**步骤 20** — 调用 `get_links`，获取 `_AI测试/测试文件A.md` 的出站链接
**步骤 21** — 调用 `get_backlinks`，获取引用 `_AI测试/测试文件A.md` 的文件列表

---

## 第四阶段：搜索与发现（5 个）

**步骤 22** — 调用 `search_vault`，搜索关键词 "测试文件"
**步骤 23** — 调用 `search_by_tag`，搜索标签 "测试"
**步骤 24** — 调用 `search_by_property`，搜索 frontmatter 属性 `status` 值为 `active` 的文件
**步骤 25** — 调用 `get_recent_files`，获取最近打开的文件列表
**步骤 26** — 调用 `get_vault_graph`，获取 vault 的链接图谱概览

---

## 第五阶段：笔记与任务（3 个）

**步骤 27** — 调用 `create_note`，创建新笔记 `_AI测试/测试笔记.md`，内容为 "# 测试笔记\n测试内容"
**步骤 28** — 调用 `append_to_daily`，向今日日记追加 "- AI工具测试进行中"
**步骤 29** — 调用 `get_tasks`，列出 vault 中所有未完成的任务

---

## 第六阶段：编辑器操作（7 个）

**步骤 30** — 调用 `open_file`，打开 `_AI测试/测试文件A.md`
**步骤 31** — 调用 `get_cursor_info`，获取当前光标位置信息
**步骤 32** — 调用 `get_editor_content`，获取当前编辑器内容
**步骤 33** — 调用 `insert_at_cursor`，在光标位置插入 "\n## 插入测试\n光标插入内容"
**步骤 34** — 调用 `replace_selection`，替换选中文本为 "替换后的内容"
**步骤 35** — 调用 `apply_formatting`，对当前选中文本应用加粗格式
**步骤 36** — 调用 `scroll_editor`，滚动到第 1 行

---

## 第七阶段：工作区操作（4 个）

**步骤 37** — 调用 `get_open_files`，列出所有打开的文件
**步骤 38** — 调用 `show_panel`，打开图谱视图
**步骤 39** — 调用 `execute_command`，执行命令 `app:go-back`
**步骤 40** — 调用 `close_tab`，关闭 `_AI测试/测试笔记.md` 的标签页

---

## 第八阶段：清理（1 个）

**步骤 41** — 调用 `delete_file`，删除 `_AI测试/测试文件C.md`（确认删除）

---

## 测试报告模板

执行完所有步骤后，请按以下格式输出测试报告：

```markdown
# AI 智能体工具测试报告

**测试日期**: YYYY-MM-DD
**模型**: （使用的模型名称）
**总计**: 41 个工具

## 测试结果汇总

| 序号 | 工具名称 | 类别 | 状态 | 备注 |
|------|----------|------|------|------|
| 1 | get_vault_info | 系统 | ✅/❌ | |
| 2 | get_current_datetime | 系统 | ✅/❌ | |
| ... | ... | ... | ... | ... |

## 统计

- ✅ 通过: XX 个
- ❌ 失败: XX 个
- 成功率: XX%

## 失败详情

（如有失败，列出具体错误信息和可能原因）

## 综合评价

（对工具系统的整体评价）
```

请开始按步骤执行，完成后输出测试报告。
